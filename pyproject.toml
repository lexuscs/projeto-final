[project]
name = "projeto-final"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lexuscs", email = "axl.exe@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.6",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
projeto-final = "projeto_final:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
